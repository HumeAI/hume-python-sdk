[pytest]
log_cli = 1
log_cli_level = INFO
markers =
    asyncio: Uses async features
    batch: Uses the Hume batch API
    notebook: Example notebook tests
    microphone: Uses microphone playback
    service: Authenticates with Hume APIs
    stream: Uses the Hume streaming API
